import { League_Spartan } from 'next/font/google';

const LogoFont = League_Spartan({weight: '800', subsets: ['latin']})

const Logo = ({ ...props }) => (

  <span>poddiy</span>

);

export default Logo;
